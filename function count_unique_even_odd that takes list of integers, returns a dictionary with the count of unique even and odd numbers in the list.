def count_unique_even_odd(l: list)-> dict:
    d={'even':0 , 'odd':0}
    s=set(l)
    l1=list(s)
    for i in range(len(l1)):
        if l1[i]%2==0:
            d['even']+=1
        else:
            d['odd']+=1
    return (d)        
